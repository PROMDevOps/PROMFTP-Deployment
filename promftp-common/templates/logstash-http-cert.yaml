apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: logstash-http-cert
spec:
  secretName: logstash-http-cert
  dnsNames:
    - {{ .Values.domainHttp }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
